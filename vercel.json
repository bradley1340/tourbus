{
  "version": 2,
  "buildCommand": "npm install && node --max-old-space-size=4096 ./node_modules/.bin/vite build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
